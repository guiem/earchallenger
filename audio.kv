#:kivy 1.0
#:import kivy kivy

<AudioBackground>:
    canvas:
        Color:
            rgb: 1, 1, 1
        Rectangle:
            source: 'data/images/background.jpg'
            size: self.size

    BoxLayout:
        padding: 10
        spacing: 10
        size_hint: 1, None
        pos_hint: {'top': 1}
        height: 44
        PlayButton:
            text:"Play"
            font_size: '25sp'
            on_press: app.play_sequence()
        NextButton:
            text:"Next"
            font_size: '25sp'
            on_press: app.next_sequence()
        AnswerButton:
            text:"Answer"
            font_size: '25sp'
            on_press: app.answer_sequence(answer_label)
        Label:
            height: 24
            text_size: self.size
            color: (1, 1, 1, .8)
            text: ''
            valign: 'middle'
            id: answer_label


